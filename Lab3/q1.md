Binary Search

At every step:

n → n/2 → n/4 → n/8 → ...

Recurrence:

T(n)=T(n/2)+O(1)

Therefore:

T(n)=O(log
2
	​

n)
Ternary Search

At every step:

n → n/3 → n/9 → n/27 → ...

But we need two comparisons to determine which of the three portions contains the element.

T(n)=T(n/3)+O(1)

Therefore:

T(n)=O(log
3
	​

n)

Although log
3
	​

n<log
2
	​

n, ternary search performs more comparisons per iteration. Hence binary search is generally faster for searching an array.

Binary search is preferable because each iteration requires fewer comparisons, even though ternary search reduces the search space to one-third.